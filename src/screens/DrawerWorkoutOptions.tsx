import React from 'react'
import { Text } from 'react-native'
import styled from 'styled-components'

const Container = styled.View``

type Props = {}

export const DrawerWorkoutOptions: React.FC<Props> = () => {
  return (
    <Container>
      <Text>DrawerWorkoutOptions</Text>
    </Container>
  )
}
